#Types 

type Blogs{
    _id: ID!,
    Content: String!,
    Title: String!,
    Photo: String!,
    Bookmarks: Boolean!
    Likes: Int
}

#Mutations Types

input BlogCreate{
    Content: String!,
    Title: String!,
    Photo: String!,
}

#Query

type Query{
    "Mira todos los Blogs / See all the blogs"
    getBlogs:[Blogs]
}

type Mutation{
    "Crear Blog / Create Blog"
    createBlog(input: BlogCreate!): Blogs

}